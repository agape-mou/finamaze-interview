/* 
    Trade-off
    Instead of fetching teams in database, we can load directly as static data 
    cause the members of teams are not changed frequently.
*/

const teams = [
  // add other teams config here
];

module.exports = teams;
